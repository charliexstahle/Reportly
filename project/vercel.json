{
  "version": 2,
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["iad1"],
  "env": {
    "NEXT_PUBLIC_SUPABASE_URL": "env var set on deployment platform",
    "NEXT_PUBLIC_SUPABASE_ANON_KEY": "env var set on deployment platform"
  }
}